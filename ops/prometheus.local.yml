global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: clinical-rag
    metrics_path: /metrics
    static_configs:
      # for running the Prometheus binary directly on the host (no Docker)
      - targets: ["localhost:8000"]
