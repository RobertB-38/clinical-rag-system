global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: clinical-rag
    metrics_path: /metrics
    static_configs:
      # scrape the API running on the host (uvicorn on :8000), not in a container
      - targets: ["host.docker.internal:8000"]
