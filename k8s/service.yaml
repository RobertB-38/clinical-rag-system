apiVersion: v1
kind: Service
metadata:
  name: clinical-rag
  labels: {app: clinical-rag}
spec:
  selector: {app: clinical-rag}
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
