# Template only — DO NOT commit a real secret. Create the live secret with:
#   kubectl create secret generic clinical-rag-secrets \
#     --from-literal=RAG_ANTHROPIC_API_KEY=sk-ant-... \
#     --from-literal=OPENAI_API_KEY=sk-...
apiVersion: v1
kind: Secret
metadata:
  name: clinical-rag-secrets
type: Opaque
stringData:
  RAG_ANTHROPIC_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
